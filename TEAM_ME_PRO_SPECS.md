# 📋 وثيقة المواصفات الشاملة لتطبيق TEAM ME PRO

## مرجع كامل لبناء نسخة React Native للهاتف المحمول

---

## 📌 1. نظرة عامة على التطبيق

| البند                | التفاصيل                                                                                                                                                 |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **الاسم**            | TEAM ME PRO (تيمي برو)                                                                                                                                   |
| **النوع**            | تطبيق ويب تقدمي (PWA) + نسخة موبايل مطلوبة                                                                                                               |
| **الوصف**            | منصة تفاعلية متكاملة لإدارة الفرق الرياضية وفرق الألعاب، توليد تشكيلات متوازنة، إدارة بطولات إقصائية، قرعة لمس أصابع تفاعلية، ومحرك اتخاذ قرارات عشوائية |
| **الجمهور المستهدف** | مجموعات كرة القدم، بادل، كرة سلة، eSports، فرق عمل، أنشطة جماعية                                                                                         |
| **اللغات**           | عربية (RTL) + إنجليزية (LTR) — تبديل فوري                                                                                                                |
| **المظاهر**          | 7 أوضاع تصميم زجاجي احترافي (Glassmorphism)                                                                                                              |
| **التخزين**          | محلي بالكامل (AsyncStorage في React Native / localStorage في الويب)                                                                                      |

---

## 🏗️ 2. خريطة البنية التقنية الحالية (Web Version)

### التقنيات المستخدمة في الويب:

```
React 19 + TypeScript + Vite 6
├── التصميم: Tailwind CSS + Vanilla CSS Design Tokens (Glassmorphism)
├── التحريك: Framer Motion
├── الأيقونات: Lucide React
├── الاحتفالات: canvas-confetti
├── الصوتيات: Web Audio API (محاكي صوتي برمجي)
├── التوجيه: React Router (HashRouter)
└── PWA: Vite PWA Plugin + Service Worker
```

### البدائل المقترحة لـ React Native:

```
React Native 0.76+ / Expo SDK 52+
├── التصميم: React Native StyleSheet + react-native-reanimated
├── التحريك: react-native-reanimated 3 + Moti
├── الأيقونات: react-native-vector-icons (Lucide set)
├── الاحتفالات: react-native-confetti-cannon
├── الصوتيات: expo-av أو react-native-sound
├── التوجيه: React Navigation 7 (Stack + Bottom Tabs)
├── التخزين: @react-native-async-storage/async-storage
├── اللمس المتعدد: react-native-gesture-handler (PanGestureHandler)
└── الخطوط: expo-font (Cairo, Inter, Orbitron)
```

---

## 🗂️ 3. هيكل الملفات والمجلدات

```
randomplay/
├── App.tsx                    # المكون الرئيسي + Navbar + Router + LanguageContext
├── types.ts                   # أنواع البيانات (Member, Team, Role, AppTheme, etc.)
├── constants.tsx              # الترجمات (AR/EN) + نماذج جاهزة (Presets) + ROLE_ICONS
├── index.css                  # Design Tokens + CSS Variables + 7 Themes + Glassmorphism
│
├── pages/
│   ├── LandingPage.tsx        # الصفحة الرئيسية (Hero + Demo + Features + Stats)
│   ├── MembersPage.tsx        # إدارة قائمة الأعضاء (CRUD + Bulk + Search + Filter)
│   ├── GeneratePage.tsx       # محرك توليد الفرق (4 خوارزميات + Swap + Export)
│   ├── TournamentPage.tsx     # نظام البطولات الإقصائية (Bracket)
│   ├── FingerChooserPage.tsx  # قرعة لمس الأصابع (Multi-Touch + Pairs + Teams)
│   ├── PickPage.tsx           # عراف القرارات العشوائية (Oracle)
│   ├── HistoryPage.tsx        # سجل التشكيلات المحفوظة
│   └── SettingsPage.tsx       # الإعدادات (Themes + Sound + Lang + Backup)
│
├── components/
│   ├── ThemeSwitcher.tsx      # مبدل المظاهر (Dropdown + Compact)
│   ├── MatchTimer.tsx         # مؤقت المباراة (5/10/15 دقيقة)
│   └── Footer.tsx             # ذيل الصفحة مع بادجات الخصوصية
│
├── contexts/
│   └── ThemeContext.tsx        # سياق المظهر (state + localStorage + data-theme)
│
├── services/
│   ├── audioService.ts        # محاكي صوتي بـ Web Audio API (click, charge, success, delete)
│   └── geminiService.ts       # خدمة AI (اختياري)
│
└── public/
    ├── pwa-192.png            # أيقونة PWA (192x192)
    └── pwa-512.png            # أيقونة PWA + لوجو الشعار (512x512)
```

---

## 📱 4. وصف تفصيلي لكل شاشة (Screen-by-Screen Specification)

---

### 📱 شاشة 1: الصفحة الرئيسية (LandingPage)

**المسار**: `/` | **الملف**: `pages/LandingPage.tsx`

#### المكونات المرئية:

1. **بادج المقدمة**: شريط صغير أعلى الصفحة يعرض وصف مختصر للمنصة.
2. **عنوان Hero ضخم**: سطران بخط Orbitron عريض مع gradient text.
3. **وصف فرعي**: فقرة تفصيلية عن إمكانيات المنصة.
4. **4 أزرار CTA رئيسية**:
   - `تـولـيد التشكيلة الآن` → `/generate`
   - `إدارة الأعضاء والنخبة` → `/members`
   - `نظام البطولات 🏆` → `/tournament`
   - `قرعة الأصابع 👆` → `/finger`
5. **كارت المعاينة التفاعلية الحية (Live Demo)**: يعرض فريقين وهميين بـ 5 لاعبين لكل فريق، مع زر "خلط الآن" يعيد توزيعهم أمام العين مع كونفيتي.
6. **إحصائيات حية**: 3 بطاقات تعرض عدد الأعضاء المسجلين + عدد الفرق المتوازنة + سرعة التوزيع.
7. **3 بطاقات ميزات**: خوارزميات التوازن / التصاميم المتعددة / مؤقت وبطولات.
8. **قسم الانطلاق السريع**: زر لتحميل نموذج جاهز من أعضاء وتوليد الفرق فوراً.

#### منطق الأعمال:

- تحميل عدد الأعضاء والفرق من `localStorage`.
- Demo shuffler يستخدم خوارزمية عشوائية مع `canvas-confetti`.

---

### 📱 شاشة 2: إدارة الأعضاء (MembersPage)

**المسار**: `/members` | **الملف**: `pages/MembersPage.tsx`

#### المكونات المرئية:

1. **شريط البحث والفلترة**: حقل بحث بالاسم + أزرار فلترة حسب الدور (الكل، قائد، مهاجم، مدرع، دعم، صانع ألعاب، مستجد).
2. **عداد الأعضاء**: يعرض العدد الإجمالي.
3. **3 أزرار عمليات**: إضافة عضو + استيراد جماعي + نماذج جاهزة.
4. **شبكة كروت الأعضاء (Grid)**: كل كارت يعرض:
   - أيقونة الدور الملونة
   - الاسم
   - الدور (بالعربي أو الإنجليزي)
   - شريط مستوى المهارة (1-10 نجوم)
   - ملاحظات (إن وجدت)
   - زر تعديل + زر حذف
5. **نموذج إضافة / تعديل عضو (Modal)**:
   - حقل الاسم (مطلوب)
   - قائمة اختيار الدور (6 أدوار)
   - شريط تمرير المهارة من 1 إلى 10
   - حقل ملاحظات اختياري
6. **نموذج الاستيراد الجماعي (Bulk Import Modal)**: منطقة نص لصق أسماء (سطر لكل اسم أو مفصولة بفواصل).
7. **نماذج جاهزة**: 3 باقات (كرة قدم 5v5 / eSports / فرق عمل) بنقرة واحدة.
8. **حالة فارغة (Empty State)**: رسالة ودية مع أيقونة وأزرار لبدء الإضافة.

#### نموذج البيانات:

```typescript
interface Member {
  id: string; // UUID فريد
  name: string; // الاسم الكامل
  role: Role; // الدور التكتيكي
  level: number; // مستوى المهارة (1-10)
  avatar?: string; // صورة رمزية (اختياري)
  notes?: string; // ملاحظات (اختياري)
}
```

---

### 📱 شاشة 3: محرك توليد الفرق (GeneratePage)

**المسار**: `/generate` | **الملف**: `pages/GeneratePage.tsx`

#### المكونات المرئية:

1. **لوحة الإعدادات**:
   - عداد عدد الفرق (2-6)
   - 4 أوضاع توزيع: عشوائي / موزون / قادة أولاً / توازن الأدوار
   - 6 إضافات: مؤقت / أصوات / لوحة نقاط / إحصائيات / كونفيتي تلقائي
2. **زر التوليد الرئيسي**: "تـولـيد الـفـرق الآن" مع أنيميشن شحن ثلاثي المراحل.
3. **عرض الفرق المولّدة**: كل فريق في كارت زجاجي يحتوي:
   - اسم الفريق ولونه
   - قائمة الأعضاء مع أيقونات الأدوار وشرائط المهارة
   - متوسط القوة + مجموع المهارات
   - عداد النقاط (إن مفعّل)
4. **التبديل التفاعلي (Swap)**: الضغط على لاعب ثم لاعب آخر يبدّلهما بين الفريقين.
5. **مؤقت المباراة (MatchTimer)**: عداد تنازلي (5/10/15 دقيقة) مع تنبيه صوتي عند الانتهاء.
6. **أزرار التصدير والحفظ**:
   - نسخ للواتساب (تنسيق نصي بإيموجي)
   - تصدير JSON
   - حفظ في السجل
   - إنشاء بطولة من الفرق الحالية

#### خوارزميات التوزيع الأربعة:

| الخوارزمية                        | الوصف                                                      |
| --------------------------------- | ---------------------------------------------------------- |
| **عشوائي بحت (Random)**           | Fisher-Yates shuffle ثم توزيع round-robin                  |
| **موزون حسب المهارة (Balanced)**  | ترتيب تنازلي حسب المهارة ثم Snake Draft (1→N, N→1, 1→N...) |
| **القادة أولاً (Captains First)** | فصل القادة وتوزيعهم فرقة فرقة ثم توزيع الباقي بالمهارة     |
| **توازن الأدوار (Role Balanced)** | تجميع حسب الدور ثم توزيع كل مجموعة بالتساوي على الفرق      |

---

### 📱 شاشة 4: نظام البطولات (TournamentPage)

**المسار**: `/tournament` | **الملف**: `pages/TournamentPage.tsx`

#### المكونات المرئية:

1. **زر إنشاء البطولة**: يأخذ الفرق الحالية ويبني جدول مواجهات إقصائي.
2. **شجرة المواجهات (Bracket Tree)**:
   - ربع النهائي ← نصف النهائي ← المباراة النهائية
   - كل مباراة تعرض: اسم الفريقين + حقول إدخال النتيجة + زر "تأهيل الفائز"
3. **تتويج البطل**: عند انتهاء النهائي يظهر كارت التتويج مع كونفيتي وتاج ذهبي وعبارة "بطل البطولة 👑".
4. **زر إعادة ضبط البطولة**.

#### نموذج البيانات:

```typescript
interface TournamentMatch {
  id: string;
  round: number; // 1=Quarter, 2=Semi, 3=Final
  teamA?: Team;
  teamB?: Team;
  scoreA: number;
  scoreB: number;
  winner?: Team;
}
```

---

### 📱 شاشة 5: قرعة لمس الأصابع (FingerChooserPage) ⭐

**المسار**: `/finger` | **الملف**: `pages/FingerChooserPage.tsx`

> [!IMPORTANT]
> هذه الشاشة هي الأكثر تعقيداً وتميزاً في التطبيق وتتطلب عناية خاصة في React Native باستخدام `react-native-gesture-handler` مع `Gesture.Simultaneous` و `Gesture.Manual`.

#### 3 أوضاع اللعب:

| الوضع                | الوصف                                         | الإدخال                 |
| -------------------- | --------------------------------------------- | ----------------------- |
| **اختيار فائزين 👑** | اختيار N فائزين عشوائياً من بين جميع الأصابع  | عداد عدد الفائزين (1-5) |
| **مين مع مين 🤝**    | ربط كل إصبعين معاً كثنائي عشوائي مع خطوط ليزر | لا إدخال إضافي          |
| **تقسيم لفرق ⚔️**    | فرز الأصابع إلى 2-4 فرق بألوان مميزة          | عداد عدد الفرق (2-4)    |

#### آلية العمل:

1. **وضع الانتظار**: شاشة كبيرة فارغة مع رسالة "ضعوا أصابعكم على الشاشة" وأيقونة نابضة.
2. **اللمس**: عند وضع كل إصبع تظهر دائرة هولوجرافية متوهجة بلون فريد + حلقات رادار نابضة + رقم اللاعب.
3. **العد التنازلي التلقائي**: عند تثبيت 2+ أصابع لمدة 2.4 ثانية يبدأ شريط تقدم + عد تنازلي 3...2...1.
4. **الاختيار**: حسب الوضع المختار (تتويج / ثنائيات / فرق) مع أنيميشن مبهرة.
5. **الاحتفال**: كونفيتي + صوت نصر + تاج ذهبي للفائزين.
6. **محاكاة الكمبيوتر**: زر "محاكاة أصابع للتجربة" يضع 4-6 نقاط وهمية.

#### حماية اللمس المتعدد على الموبايل (مطبقة):

- منع pinch-to-zoom و double-tap zoom و context menu و pull-to-refresh
- `touch-action: none` و `user-scalable=no` على مستوى الصفحة
- `{ passive: false }` على جميع `touchstart`/`touchmove` listeners

#### في React Native:

```
استخدم react-native-gesture-handler مع:
- Gesture.Manual() لتتبع كل إصبع على حدة
- Gesture.Simultaneous() لدعم اللمس المتعدد
- react-native-reanimated لرسم الحلقات والدوائر المتحركة
- react-native-svg لخطوط الليزر في وضع الثنائيات
```

---

### 📱 شاشة 6: عراف القرارات (PickPage / Oracle)

**المسار**: `/pick` | **الملف**: `pages/PickPage.tsx`

#### المكونات المرئية:

1. **حقل إدخال الخيارات**: اكتب خياراً ثم Enter لإضافته (مثل: أسماء لاعبين، أماكن، ألعاب).
2. **قائمة الخيارات النشطة**: كل خيار يظهر كشريحة (Chip) ملونة مع زر حذف.
3. **أمثلة جاهزة**: "من يبدأ أولاً؟" / "ماذا نأكل اليوم؟" / "أي لعبة نلعب؟"
4. **زر بدء الاختيار العشوائي**: يشغل أنيميشن شحن ثلاثية المراحل (تحليل ← فحص ← اختيار).
5. **عرض النتيجة**: كارت كبير يعرض الخيار المحسوم مع تأثير هالة مشعة ومؤثر صوتي.
6. **زر إعادة السحب**.

---

### 📱 شاشة 7: سجل التشكيلات (HistoryPage)

**المسار**: `/history` | **الملف**: `pages/HistoryPage.tsx`

#### المكونات المرئية:

1. **قائمة التشكيلات المحفوظة**: كل عنصر يعرض:
   - التاريخ والوقت
   - عدد الأعضاء
   - وضع التوزيع المستخدم
   - زر "استعادة" + زر "حذف"
2. **زر مسح السجل بالكامل** مع تأكيد أمان.
3. **حالة فارغة** إذا لم تكن هناك تشكيلات محفوظة.

---

### 📱 شاشة 8: الإعدادات (SettingsPage)

**المسار**: `/settings` | **الملف**: `pages/SettingsPage.tsx`

#### الأقسام:

1. **معرض المظاهر السبعة**: شبكة بطاقات معاينة لكل مود مع اسمه ووصفه وألوان تمثيلية + زر تطبيق.
2. **المؤثرات الصوتية**: زر تفعيل/كتم مع مؤشر بصري.
3. **لغة التطبيق**: زر تبديل عربي ↔ إنجليزي.
4. **النسخ الاحتياطي**:
   - تصدير كافة البيانات (JSON download)
   - استيراد بيانات سابقة (file upload + parse)
5. **إعادة ضبط المصنع**: زر حذف جميع البيانات مع مودال تأكيد تحذيري.
6. **حول التطبيق**: اسم التطبيق والإصدار ووصف مختصر.

---

## 🎨 5. نظام المظاهر السبعة (Theme System)

كل مود يُعرّف مجموعة من الـ CSS Variables (Design Tokens):

| المتغير            | الوظيفة                    |
| ------------------ | -------------------------- |
| `--bg-base`        | لون الخلفية الأساسية       |
| `--bg-surface`     | سطح الطبقة الثانية         |
| `--bg-card`        | خلفية البطاقات الزجاجية    |
| `--bg-card-hover`  | خلفية البطاقات عند التحويم |
| `--border-subtle`  | حدود خفيفة                 |
| `--border-focus`   | حدود التركيز               |
| `--text-primary`   | لون النص الرئيسي           |
| `--text-secondary` | لون النص الثانوي           |
| `--text-muted`     | لون النص الباهت            |
| `--accent-primary` | لون التمييز / الأزرار      |
| `--badge-bg`       | خلفية الشارات              |
| `--nav-bg`         | خلفية شريط التنقل          |
| `--input-bg`       | خلفية حقول الإدخال         |
| `--glass-shadow`   | ظل الزجاج                  |

### الأوضاع السبعة:

1. 🔮 **nebula** — Mystic Violet & Royal Blue (الافتراضي)
2. ❄️ **frost** — Pure Frost Light (فاتح)
3. 💎 **corporate** — Crystal Obsidian (داكن)
4. 👑 **royal** — Imperial Gold & Amber
5. 🌌 **aurora** — Cosmic Emerald & Mint
6. **cyber** — Neon HUD Electric
7. 🌸 **crimson** — Velvet Rose & Ruby

#### في React Native:

```typescript
// استخدم Context + StyleSheet.create مع كائن themes
const themes = {
  nebula: { bgBase: '#070817', textPrimary: '#f5f3ff', accentPrimary: '#7c3aed', ... },
  frost:  { bgBase: '#f1f5f9', textPrimary: '#0f172a', accentPrimary: '#3b82f6', ... },
  // ... باقي المظاهر
};
```

---

## 🔊 6. نظام المؤثرات الصوتية (Audio Service)

محاكي صوتي برمجي بالكامل يستخدم **Web Audio API OscillatorNode** بدون أي ملفات صوتية:

| الدالة                 | الوظيفة              | التردد              |
| ---------------------- | -------------------- | ------------------- |
| `playClick(freq, vol)` | صوت نقرة خفيفة       | 800Hz sine, 50ms    |
| `playTab()`            | صوت تبديل تبويب      | 600Hz + 900Hz, 80ms |
| `playCharge(duration)` | صوت شحن حماسي تصاعدي | 200→1200Hz sweep    |
| `playSuccess()`        | صوت نصر وتتويج       | C5→E5→G5 arpeggio   |
| `playDelete()`         | صوت حذف تنازلي       | 400→100Hz drop      |
| `toggle()`             | تشغيل / كتم          | —                   |
| `isEnabled()`          | حالة الصوت           | —                   |

#### في React Native:

```
استخدم expo-av أو react-native-sound لتشغيل ملفات صوتية .wav/.mp3 مقابلة لكل تأثير.
بديل: استخدام expo-audio-api لإنشاء أصوات برمجية مشابهة.
```

---

## 🌐 7. نظام الترجمة (i18n)

ملف `constants.tsx` يحتوي كائن `TRANSLATIONS` مع مفتاحين:

- `ar` — العربية
- `en` — الإنجليزية

كل قسم من التطبيق له مفاتيح فرعية:

```
nav, landing, members, roles, generate, tournament, finger, oracle, history, settings, timer, footer
```

#### في React Native:

```
استخدم i18next + react-i18next أو expo-localization مع نفس ملفات الترجمة.
```

---

## 💾 8. نموذج البيانات والتخزين

### الكائنات المخزنة في localStorage:

| المفتاح            | النوع            | الوصف                       |
| ------------------ | ---------------- | --------------------------- |
| `tf_members`       | `Member[]`       | قائمة جميع الأعضاء المسجلين |
| `tf_current_teams` | `Team[]`         | الفرق المولّدة حالياً       |
| `tf_lang`          | `'ar' \| 'en'`   | اللغة المختارة              |
| `tf_theme`         | `AppTheme`       | المظهر المختار              |
| `tf_sound`         | `boolean`        | حالة الصوت                  |
| `tf_history`       | `SavedHistory[]` | سجل التشكيلات المحفوظة      |

#### في React Native:

```typescript
import AsyncStorage from "@react-native-async-storage/async-storage";
// نفس المفاتيح والقيم مع JSON.stringify/parse
```

---

## 📲 9. ملاحظات تقنية لتحويل React Native

### الشريط العلوي (Navigation Bar):

- استخدم `@react-navigation/bottom-tabs` لشريط تنقل سفلي مع أيقونات.
- أو `@react-navigation/drawer` لقائمة جانبية.
- التبويبات: الرئيسية | الأعضاء | توليد | البطولة | الأصابع | العراف | السجل | الإعدادات.

### التحريك:

- `react-native-reanimated` بديل لـ Framer Motion.
- `Moti` (مبني على reanimated) يوفر API مشابه لـ motion.div.

### الزجاج والتأثيرات البصرية (Glassmorphism):

- `@react-native-community/blur` للـ backdrop blur.
- أو `expo-blur` مع `BlurView`.
- `react-native-linear-gradient` للتدرجات.

### قرعة الأصابع (Finger Chooser):

- هذه أهم شاشة للتحويل.
- استخدم `GestureDetector` من `react-native-gesture-handler`.
- `Gesture.Manual()` لتتبع pointer events يدوياً (pointerDown, pointerMove, pointerUp).
- `Gesture.Simultaneous()` لدمج عدة أصابع في نفس الوقت.
- `react-native-svg` لرسم خطوط الليزر في وضع الثنائيات.

### PWA → Native:

- لا حاجة لـ Service Worker أو manifest في Native.
- استخدم `expo-splash-screen` لشاشة البداية.
- استخدم `expo-app-loading` لتحميل الخطوط.

---

## 💡 10. اقتراحات ميزات إضافية لنسخة React Native

### 🔥 ميزات أساسية مقترحة:

| #   | الميزة                                     | الوصف                                                              | الأولوية |
| --- | ------------------------------------------ | ------------------------------------------------------------------ | -------- |
| 1   | **إشعارات دفع (Push Notifications)**       | تذكير بموعد المباراة أو البطولة مع إمكانية جدولة الإشعارات         | عالية    |
| 2   | **مشاركة التشكيلة كصورة (Image Export)**   | تحويل كارت الفريق إلى صورة PNG جاهزة للمشاركة عبر واتساب وانستغرام | عالية    |
| 3   | **صور الأعضاء (Avatar Camera)**            | التقاط صورة من الكاميرا أو اختيار من المعرض لكل عضو                | عالية    |
| 4   | **نظام الدعوات والمشاركة (Invite System)** | إرسال رابط دعوة للأعضاء للانضمام تلقائياً إلى القائمة              | متوسطة   |
| 5   | **اهتزاز حسي (Haptic Feedback)**           | اهتزاز الهاتف عند اللمس، الاختيار، والتتويج في قرعة الأصابع        | عالية    |
| 6   | **وضع بدون اتصال (Offline Mode)**          | التطبيق يعمل بالكامل بدون إنترنت (موجود أصلاً لكن يمكن تعزيزه)     | متوسطة   |
| 7   | **Widget شاشة رئيسية**                     | عرض إحصائية سريعة أو اختصار لتوليد الفرق من الشاشة الرئيسية        | منخفضة   |

### 🎮 ميزات تفاعلية متقدمة:

| #   | الميزة                                         | الوصف                                                               |
| --- | ---------------------------------------------- | ------------------------------------------------------------------- |
| 8   | **حركة هز الهاتف (Shake to Shuffle)**          | هز الهاتف لخلط الفرق أو اختيار عشوائي جديد عبر Accelerometer        |
| 9   | **بطاقات NFC**                                 | مسح بطاقة NFC لكل لاعب لتسجيل حضوره تلقائياً                        |
| 10  | **QR Code Scanner**                            | مسح QR Code لاستيراد قائمة أعضاء أو تشكيلة كاملة                    |
| 11  | **دردشة المجموعة (Group Chat)**                | غرفة دردشة بسيطة مرتبطة بالفرق المولّدة                             |
| 12  | **خريطة الملاعب (Map Integration)**            | عرض أقرب الملاعب والأماكن المتاحة للعب على الخريطة                  |
| 13  | **تتبع الإحصائيات التاريخية**                  | رسوم بيانية لأداء كل لاعب عبر المباريات (الفوز، الخسارة، المشاركات) |
| 14  | **الاتصال المباشر بين الأجهزة (Peer-to-Peer)** | مزامنة القوائم بين هواتف المجموعة عبر Bluetooth أو WiFi Direct      |

### 🏆 ميزات البطولات المتقدمة:

| #   | الميزة                        | الوصف                                                                   |
| --- | ----------------------------- | ----------------------------------------------------------------------- |
| 15  | **جدول الدوري (League Mode)** | بالإضافة للإقصائي، إضافة نظام دوري (كل فريق يلعب ضد الكل) مع جدول ترتيب |
| 16  | **إحصائيات البطولة**          | هدافين، أفضل لاعب، أكثر فوز، متوسط الأهداف                              |
| 17  | **جدولة المباريات**           | ربط كل مباراة بتاريخ ووقت مع تذكيرات                                    |
| 18  | **بث مباشر للنتائج**          | مشاركة رابط حي لجدول البطولة مع المتابعين                               |

### 💎 ميزات تجربة المستخدم:

| #   | الميزة                      | الوصف                                                          |
| --- | --------------------------- | -------------------------------------------------------------- |
| 19  | **Onboarding تعريفي**       | شاشات ترحيب عند أول استخدام تشرح الميزات الرئيسية              |
| 20  | **وضع الليل التلقائي**      | التبديل بين المظهر الفاتح والداكن حسب إعدادات النظام           |
| 21  | **أنيميشن Lottie**          | استبدال أيقونات CSS المتحركة بـ Lottie animations لأداء أفضل   |
| 22  | **تقييم التطبيق (Rate Us)** | مطالبة المستخدم بتقييم التطبيق على المتجر بعد استخدامات متعددة |

---

## 🚀 11. خطة التنفيذ المقترحة لـ React Native

### المرحلة 1 — الأساسيات (أسبوع 1-2):

- [ ] إعداد المشروع بـ Expo SDK 52
- [ ] تنفيذ نظام المظاهر السبعة مع ThemeContext
- [ ] بناء شاشة الإعدادات (Themes + Sound + Lang + Backup)
- [ ] بناء Bottom Tab Navigator مع 8 شاشات
- [ ] تطبيق نظام i18n (عربي/إنجليزي + RTL/LTR)

### المرحلة 2 — إدارة الأعضاء والتوليد (أسبوع 3-4):

- [ ] بناء شاشة إدارة الأعضاء (CRUD + Bulk + Presets)
- [ ] بناء محرك توليد الفرق (4 خوارزميات)
- [ ] تنفيذ مؤقت المباراة ولوحة النقاط
- [ ] تنفيذ تصدير واتساب ومشاركة كصورة
- [ ] بناء سجل التشكيلات

### المرحلة 3 — البطولات والقرارات (أسبوع 5-6):

- [ ] بناء شاشة البطولات الإقصائية
- [ ] بناء عراف القرارات
- [ ] تنفيذ الصفحة الرئيسية مع المعاينة الحية

### المرحلة 4 — قرعة الأصابع ⭐ (أسبوع 7-8):

- [ ] بناء شاشة قرعة لمس الأصابع مع react-native-gesture-handler
- [ ] تنفيذ الأوضاع الثلاثة (فائزين / ثنائيات / فرق)
- [ ] تنفيذ Haptic Feedback + Sound + Confetti
- [ ] اختبار على أجهزة حقيقية (iOS + Android)

### المرحلة 5 — تلميع ونشر (أسبوع 9-10):

- [ ] Onboarding تعريفي
- [ ] اختبارات وتحسينات أداء
- [ ] بناء ونشر على App Store + Google Play

---

## 📦 12. المكتبات المطلوبة لـ React Native (package.json)

```json
{
  "dependencies": {
    "expo": "~52.0.0",
    "@react-navigation/native": "^7.0.0",
    "@react-navigation/bottom-tabs": "^7.0.0",
    "@react-navigation/stack": "^7.0.0",
    "react-native-reanimated": "^3.16.0",
    "react-native-gesture-handler": "^2.20.0",
    "react-native-safe-area-context": "^5.0.0",
    "react-native-screens": "^4.0.0",
    "react-native-svg": "^15.0.0",
    "@react-native-async-storage/async-storage": "^2.1.0",
    "expo-av": "~14.0.0",
    "expo-haptics": "~13.0.0",
    "expo-blur": "~13.0.0",
    "expo-font": "~12.0.0",
    "expo-splash-screen": "~0.27.0",
    "expo-localization": "~15.0.0",
    "i18next": "^23.0.0",
    "react-i18next": "^15.0.0",
    "react-native-linear-gradient": "^2.8.0",
    "react-native-confetti-cannon": "^1.5.2",
    "react-native-vector-icons": "^10.0.0",
    "moti": "^0.29.0",
    "@expo/vector-icons": "^14.0.0"
  }
}
```

---

<div align="center">

**هذا المستند هو المرجع الشامل لبناء نسخة React Native كاملة من TEAM ME PRO**
آخر تحديث: أغسطس 2026

</div>
